# 小沫数字人 — 二期计划（2026-08-14）

## 目标

1. **授权后热词唤醒**：开启「听小沫」后，说出「小沫」唤出面板并进入听令  
2. **对接现有 Agent**：网页侧走 `handle_agent_text` / coordinator（分析师、拆单等）  
3. **执行安全**：网页 `staff_id=avatar_web`；开单确认仍走原执行流文案，不静默下单  

## 非目标（三期）

- Porcupine 商业热词 SDK  
- 真半身 3D / 云端 TTS  
- 私有化 ASR  

## 任务

### T1：计划（本文件） — 已完成
### T2：`avatar_agent` 接 Agent + 唤醒后指令清洗 — 已完成
### T3：前端唤醒开关 + continuous 听「小沫」 — 已完成
### T4：冒烟 — 后端规则/Agent 冒烟已过；浏览器麦需本机手动点一次

## 验收

1. 打开「听小沫」→ 授权麦 → 说「小沫」→ 面板打开并打招呼（浏览器手动）  
2. 说「小沫，介绍币种监视」→ 模块介绍（后端 `strip_wake` + feature 已验）  
3. 「@美股分析师 NVDA」或「分析一下 NVDA」→ Agent markdown 回复（后端 `agent_ok=True` 已验）  
