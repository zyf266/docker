---
description: 本项目沿用全局 Agent 工作流（见用户目录 ~/.cursor/rules/agent-workflow.mdc）
alwaysApply: false
---

# 工作流

全局五角色 Slash 命令与 `/pipeline` 已配置在 `~/.cursor/rules/agent-workflow.mdc` 与 `~/.cursor/skills/`。

本项目 `docs/plans/` 目录用于 `/plan` 产出计划文档。
